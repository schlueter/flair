language: python
python: '2.7'
install:
  pip install -U ansible-lint==2.1.3 'ansible<=1.9.9' requests
script:
- >
  find . -maxdepth 1 -name '*.yml'| xargs -n1
  ansible-playbook --syntax-check -i 'localhost,'
- >
  find . -maxdepth 1 -name '*.yml'| xargs -n1
  ansible-lint
